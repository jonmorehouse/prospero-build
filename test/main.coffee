chai = require "chai" #require chai assertions
mocha = require "mocha" #allows for testing of html with using browser / coffeescript

# allow the should method to be accessible by our prototype
do chai.should


# name = "asdf"

# behavior driven style
# name.should.equal "Jon"  

# test driven style
# chai.expect(name).to.equal "Jon test 2"
